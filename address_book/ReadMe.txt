The Project will Run on any Shell
Install Django in the shell for the execution of Project("pip install django")
No need of any superuser cos user can already input datas
even though super-user can be created by ("python manage.py createsuperuser")
